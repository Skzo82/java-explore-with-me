spring:
  datasource:
    url: jdbc:postgresql://localhost:5544/ewm_main
    username: ewm
    password: ewm
  jpa:
    hibernate:
      ddl-auto: validate
    open-in-view: false
  flyway:
    enabled: true
    locations: classpath:db/migration

server:
  port: 8080