server:
  port: 9090

spring:
  datasource:
    url: jdbc:postgresql://localhost:5433/ewm_stats
    username: ewm
    password: ewm
  jpa:
    hibernate:
      ddl-auto: validate
    open-in-view: false
  flyway:
    enabled: true
    locations: classpath:db/migration